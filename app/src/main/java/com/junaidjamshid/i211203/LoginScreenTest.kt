package com.junaidjamshid.i211203
/*
import androidx.test.espresso.Espresso.onView
import androidx.test.espresso.action.ViewActions.click
import androidx.test.espresso.assertion.ViewAssertions.matches
import androidx.test.espresso.matcher.ViewMatchers.*
import androidx.test.ext.junit.rules.ActivityScenarioRule
import org.junit.Rule
import org.junit.Test

class LoginScreenTest {

    @get:Rule
    val activityRule = ActivityScenarioRule(LoginScreem::class.java)

    @Test
    fun testNavigationToSignUpScreen() {
        // Click on the register link
        onView(withId(R.id.registerLink)).perform(click())

        // Verify that Sign-Up Screen is displayed
        onView(withId(R.id.signUpScreenLayout)).check(matches(isDisplayed()))
    }
}
*/